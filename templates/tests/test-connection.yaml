apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insta-insights.fullname" . }}-test-connection"
  labels:
    {{- include "insta-insights.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insta-insights.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
