apiVersion: v1
kind: Service
metadata:
  name: {{ include "insta-insights.fullname" . }}
  labels:
    {{- include "insta-insights.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insta-insights.selectorLabels" . | nindent 4 }}
